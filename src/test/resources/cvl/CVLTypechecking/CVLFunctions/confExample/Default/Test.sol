contract Test {
    constructor() public { }
    uint256 a;
    function yeet(uint256 x) public pure returns (uint256) {
        return 5;
    }
    function mcGeet(uint256 x) public pure returns (uint256) {
        return x;
    }
}